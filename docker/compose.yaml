services:
  bot:
    image: ${YUQA_IMAGE:-yuqa:latest}
    env_file:
      - .env
    environment:
      YUQA_DATA_DIR: /data
    volumes:
      - yuqa-data:/data
    restart: unless-stopped

volumes:
  yuqa-data:
